# Update "Check my outfit" button copy

Replace the current outfit-check labels with the parent-centered option the user selected.

## Changes
- In `src/components/today-screen/index.tsx`, change the bottom action button text from **"Check my outfit"** to **"Check baby's outfit"**.
- In `src/components/today-screen/check-outfit-panel.tsx`, change the panel title from **"Check my outfit"** to **"Check baby's outfit"** so the label stays consistent when the panel opens.

## Outcome
The Today screen button and the check-outfit panel both read "Check baby's outfit", matching the warmer, parent-focused voice the user chose.
